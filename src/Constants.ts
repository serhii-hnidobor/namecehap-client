/**
 * @module Constants
 */

/**
 * @constant
 * A api version
 */
export const API_VERSION = 'v1.0';

/**
 * @constant
 * A Default base url for a request
 */
export const NAMECHEAP_BASE_URL
  = 'https://api.sandbox.namecheap.com/xml.response';

/**
 * @constant
 * A global param clientIp max length
 */

export const CLIENT_IP_MAX_LENGTH = 15;

/**
 * @constant
 * A global param apiUser max length
 */

export const API_USER_MAX_LENGTH = 20;

/**
 * @constant
 * A global param apiKey max length
 */

export const API_KEY_MAX_LENGTH = 50;

/**
 * @constant
 * A global param apiKey max length
 */

export const USER_NAME_MAX_LENGTH = 20;
